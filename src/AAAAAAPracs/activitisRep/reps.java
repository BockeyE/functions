package AAAAAAPracs.activitisRep;

/**
 * @author bockey
 */
public class reps {

    {
    //获取所有流程图中usertask
//        BpmnModel bpmnModel = repositoryService.getBpmnModel("流程定义的ID");
//        //process中包含所有的节点信息,包括流程线

//        Task singleResult = taskService.createTaskQuery().processInstanceId(processId).singleResult();

//        Process process = bpmnModel.getProcessById(singleResult.getProcessDefinitionId());
//        //获取第一个节点信息
//        FlowElement startElement = process.getInitialFlowElement();
//        List<UserTask> userTaskList=new ArrayList<>();
//        //筛选流程中所有的UserTask任务节点
//        for (FlowElement flowElement:flowElements){
//            if (flowElement instanceof UserTask){
//                UserTask userTask= ((UserTask) flowElement);
//                userTaskList.add(userTask);
//            }
//        }

    }


}
